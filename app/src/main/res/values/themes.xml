<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.FastDo" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/app_color</item>
        <item name="colorPrimaryVariant">@color/app_color</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.FastDo.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.FastDo.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.FastDo.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="spinner_style">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_margin">30dp</item>

        <item name="android:popupBackground">#DFFFFFFF</item>
    </style>

    <style name="EditText">
        <item name="android:layout_margin">@dimen/edit_text_margin</item>
        <item name="android:textColorHint">@color/app_color</item>

    </style>

    <style name="ButtonNext">
        <item name="android:text">@string/next</item>
        <item name="android:enabled">true</item>
    </style>

    <style name="toolbar">
        <item name="android:background">@color/app_color</item>
        <item name="android:paddingStart">@dimen/toolbar_padding</item>
        <item name="android:paddingLeft">@dimen/toolbar_padding</item>
        <item name="android:paddingRight">@dimen/toolbar_padding</item>
        <item name="android:paddingEnd">@dimen/toolbar_padding</item>

    </style>

    <style name="toolbar_image">
        <item name="android:background">@drawable/fast_do_background_rounded</item>
        <item name="android:src">@drawable/company</item>
        <item name="android:scaleType">fitCenter</item>
    </style>

    <style name="stagnant_btns_fragment">
        <item name="android:padding">@dimen/add_stagnant_padding</item>
        <item name="android:textColor">@color/unactivated_fragment_btn</item>
    </style>

    <style name="Theme.FastDo.NoActionBar.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.FastDo.NoActionBar.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.FastDo.NoActionBar.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>